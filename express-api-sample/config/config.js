// Configuration file (e.g., for port, db, etc.)

// config/config.js

module.exports = {
    port: 3001,  // we can change this to any port number
    env: 'development',  // 'production', 'development', etc.
  };
  